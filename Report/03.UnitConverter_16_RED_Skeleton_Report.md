# UnitConverter_16 — RED Skeleton Report

> Phase: **RED** | Layer: **entity** | Track: **Logic**  
> Decision: **D-LOC-01** (FR-1)  
> 날짜: 2026-06-11

---

## 1. 개요

| 항목 | 내용 |
|------|------|
| 목적 | D-LOC-01 RED 스켈레톤 작성 및 의도적 실패 확인 |
| Test ID | D-LOC-01 (T-D-LOC-01-01 ~ 03) |
| 규칙 | AAA 주석 · `pytest.fail` only · assert/skip/xfail 금지 · `src/` 미수정 |
| 상태 | **collection error** — `src` 모듈 미존재 (RED 예상) |

---

## 2. C2C 추적

### PRD FR-1 인용

| ID | 요구사항 | 예시 |
|----|----------|------|
| FR-1 | `단위:값` 입력 파싱 | `meter:2.5` |

### D-LOC-01 판단

`find_blank_coords` — G1 row-major 격자에서 blank(0) 좌표 **위치 탐색** (1-index, row-major).  
상수 `34 / 16 / 4` → `GRID_WIDTH / GRID_HEIGHT / G1_GRID_SIZE`

### Test ID → 스켈레톤 매핑

| Test ID | 함수명 | Then (의도) |
|---------|--------|-------------|
| T-D-LOC-01-01 | `test_d_loc_01_blank_coords_row_major` | `[(2,2),(3,3)]` |
| T-D-LOC-01-02 | `test_d_loc_01_valid_meter_colon_splits_unit_and_value` | row-major 순서 |
| T-D-LOC-01-03 | `test_d_loc_01_first_colon_only_splits_value_with_inner_colon` | 복수 blank |

---

## 3. 생성 파일

| 파일 | 역할 |
|------|------|
| `tests/conftest.py` | `grid_g1` 픽스처 — 0이 2개, (2,2)·(3,3) |
| `tests/entity/test_d_loc_01.py` | RED 3함수 — AAA + `pytest.fail` |

### conftest — G1 격자

```python
# src.entity.constants import: GRID_WIDTH=34, GRID_HEIGHT=16, G1_GRID_SIZE=4
grid[1][1] = 0  # (2,2) 1-index
grid[2][2] = 0  # (3,3) 1-index
```

### 테스트 패턴

```python
def test_d_loc_01_blank_coords_row_major(grid_g1):
    # Given: G1 격자 (0이 2개)
    # When: find_blank_coords(grid_g1) 호출
    find_blank_coords(grid_g1)
    # Then: [(2,2),(3,3)] 반환 (1-index, row-major)
    pytest.fail("RED: D-LOC-01 — find_blank_coords 미구현, 의도적 실패")
```

---

## 4. pytest 실행 보고

### 명령

```bash
python -m pytest tests/entity/test_d_loc_01.py::test_d_loc_01_blank_coords_row_major -v
```

### 결과

| 항목 | 값 |
|------|-----|
| Test ID | D-LOC-01 |
| exit code | **4** (collection error) |
| FAIL 한 줄 | `ModuleNotFoundError: No module named 'src'` |
| 발생 위치 | `tests/conftest.py:3` — `from src.entity.constants import ...` |
| `pytest.fail` 도달 | ❌ (import 단계에서 중단) |

### 원인

`src/` 수정 금지 규칙 준수 → `src.entity.constants`, `src.entity.loc` 미생성.

---

## 5. ECB·Mock 점검

| 항목 | 결과 |
|------|------|
| Logic Track → Domain Mock 금지 | ✅ |
| entity emit 금지 | ✅ |
| assert 본문 | ❌ 없음 (준수) |
| skip / xfail | ❌ 없음 (준수) |

---

## 6. GREEN 단계 선행 조건

| 순서 | 작업 |
|------|------|
| 1 | `src/entity/constants.py` — `GRID_WIDTH=34`, `GRID_HEIGHT=16`, `G1_GRID_SIZE=4` |
| 2 | `src/entity/loc.py` — `find_blank_coords(grid) -> list[tuple]` |
| 3 | pytest 재실행 → `pytest.fail` RED 확인 → assert 구현 → GREEN |

---

## 7. 관련 문서

- `Report/02.UnitConverter_16_TDD_RED_DualTrack_Report.md`
- `docs/PRD.md`
- `.cursor/commands/red-test.md`
- `.cursor/commands/red-test-plan.md`
- `Prompt/transcript-session-export.md`
